# Include optional dependencies for sharp and other platform-specific packages
optional=true
include=optional

# Use npm registry
registry=https://registry.npmjs.org/

# Don't save exact versions
save-exact=false

# Platform configuration
platform=linux
arch=x64

# Allow scripts for sharp installation
ignore-scripts=false 